# Externa API key — never EXTERNA_API_KEY to the browser.
EXTERNA_API_URL=http://externa-core.test
EXTERNA_API_KEY=

# Collection slug to list (must allow public or key `read`).
EXTERNA_COLLECTION=posts
